name: Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to Vercel
      uses: amondnet/vercel-action@v20
      with:
        vercel-token: ${{ hJH0TLqhlNHhuT7TDKmHDCU5  }}
        vercel-org-id: ${{ l2hkWFdI1vSkFftAD4TimBAC }}
        vercel-project-id: ${{ prj_Sh7T3EkeGSuYV7nzBayacIMAMtgW }}

